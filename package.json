{
  "name": "dsh-token-stats",
  "version": "0.1.0",
  "description": "Floating draggable token usage statistics panel for DeepSeek Harness: totals, cache & hit rate, per-provider and per-model usage, current-month heatmap with per-day view, per-session detail and per-request records.",
  "type": "module",
  "main": "lib/index.js",
  "bin": {
    "dsh-token-stats": "scripts/install.js"
  },
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "scripts",
    "README.md",
    "LICENSE"
  ],
  "dsh": {
    "client": {
      "platform": "web",
      "inject": [
        "@deepseek-ai/dsh-client-runtime",
        "@deepseek-ai/dsh-client-ui-theme"
      ]
    }
  },
  "scripts": {
    "check": "node --check lib/index.js && node --check lib/client.js && node --check scripts/install.js",
    "install:local": "node scripts/install.js --from . --force"
  },
  "keywords": [
    "deepseek-harness",
    "dsh",
    "cordis",
    "plugin",
    "token",
    "usage",
    "statistics",
    "panel"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/H1a3x/dsh-token-stats.git"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "react": "^18.0.0 || ^19.0.0"
  }
}
